# Core (required for madgwick, ekf, fusion, vibration, simple map matcher)
numpy>=1.24
scipy>=1.10
matplotlib>=3.7

# Optional but recommended for full demos and analysis
pandas>=2.0

# === Heavy geo-prototyping (from 17-geopandas-map-matching.md) ===
# Uncomment only if you want to experiment with GeoPandas / OSMnx prototypes.
# These are large and not needed for the pure-numpy fusion pipeline.
# geopandas
# osmnx
# shapely
# pyproj
# leuven-map-matching

# For future PostGIS experiments (11, 15)
# psycopg2-binary
# For real SRT integration later you may also want the project's own deps
# (but sandbox is deliberately standalone)
